# App settings loaded from environment variables (never commit real values).
DATABASE_URL=postgresql+asyncpg://shramiksathi:shramiksathi@localhost:5432/shramiksathi
CORS_ORIGINS=http://localhost:3001
# §106-107 minimum wage is set by the Minimum Remuneration Fixation
# Committee every 2 years — update this floor when a new fixation is published.
MINIMUM_MONTHLY_WAGE=19550
# "Analyse with AI" (RAG over The Labour Act, 2017). Leave empty to keep the
# deterministic analysis only.
GEMINI_API_KEY=
# REQUIRED in production: long random secret used to encrypt users' digital
# signature private keys at rest. Generate with: python -c "import secrets;print(secrets.token_urlsafe(48))"
SIGNING_SECRET=
